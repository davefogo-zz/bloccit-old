<h1> Topics </h1>

<div class="row">
  <div class="col-md-8">
    <% @topics.each do |topic| %>
      <%= link_to topic.name, topic %>
      <%= topic.description %>
    <% end %>

    <%= link_to "New Topic", new_topic_path, class: 'btn btn-success' %>
  </div>
</div>
